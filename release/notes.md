# v0.1.1

更新 Markdown 解析依赖 `marked` 至 18.0.14，包含上游解析修复。组件库公开接口不变。

## 安装

```bash
pnpm add https://github.com/zyycn/codex-proxy-ui/releases/download/v0.1.1/codex-proxy-ui-0.1.1.tgz
```

需要 Vue 3.5+。包名仍为 `@codex-proxy/ui`。
